<!--
LENGTH LIMIT. This applies to everyone, and to AI-generated descriptions in particular.

The whole description must fit on one screen without scrolling. Roughly 150 to 250
words plus the test steps. If it does not fit, either the pull request is too big or
the text is padded.

The one rule that does the work: write nothing the diff already says. The reviewer
sees the changed files, the names and the lines. What they cannot see is why this
approach, what was rejected, and where to look hard. That is the entire job of this
text.

Do not paste analysis here. It belongs in a comment on the issue. Reasoning for a
specific line belongs in a code comment next to that line, where it gets maintained.
Link, do not quote.

Delete a section rather than filling it with filler.
-->

# Why we need this
<!-- Two to four sentences, from the user's perspective. Name a concrete symptom, not the solution. -->

# What is implemented
<!-- Three to six lines. Only what cannot be read off the changed file names. Do not walk through the diff. -->

# How to test
<!-- Numbered, executable steps, each with the expected result. "Tested manually" is not a step. -->
1.
2.
3.

# Closes
<!-- Use the full item URL. Issues live in sparkweavers/issues, so a bare #123 resolves against the wrong repo. -->
Closes https://github.com/sparkweavers/issues/issues/ISSUE-NUMBER
